import 'package:flutter/material.dart';

class CheckOutComplete extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
        appBar: AppBar(title: Text("Check out complete")),
        body: Text("Complete"));
  }
}
